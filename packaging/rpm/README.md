# RPM Packaging
The `.rpm` file is generated automatically via Tauri's bundler.
Simply run `scripts/build.sh` from the root directory, and the `.rpm` file will be created in `src-tauri/target/release/bundle/rpm/`.
