# AppImage Packaging
The `AppImage` file is generated automatically via Tauri's bundler.
Simply run `scripts/build.sh` from the root directory, and the `.AppImage` file will be created in `src-tauri/target/release/bundle/appimage/`.
