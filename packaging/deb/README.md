# Debian/Ubuntu Packaging
The `.deb` file is generated automatically via Tauri's bundler.
Simply run `scripts/build.sh` from the root directory, and the `.deb` file will be created in `src-tauri/target/release/bundle/deb/`.
